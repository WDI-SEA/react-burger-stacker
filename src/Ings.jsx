import React from 'react'

const Ings = (props) => {
    return (
        <p style={{backgroundColor: props.color}}>
            {props.name}
        </p>
    )
}

// TERRY CODE
// const Ings = (props) => {
//     return (
//         <p style={{backgroundColor: props.color}}>
//             {props.name}
//         </p>
//     )
// }

export default Ings