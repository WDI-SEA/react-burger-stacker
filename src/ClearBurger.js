export default function ClearBurger(props) {
        return (
            <div>
                <button
                    onClick={props.handleBurgerClear}
                >
                    Clear
                </button>
            </div>
        )
    }
