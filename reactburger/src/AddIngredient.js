import React from 'react';

const AddIngredient = (props) => {
    return (
        <div>
            <ul>
            {props.addIngredient.map{( ingredient, key) => ingredientData={props.ingredientData} addIngredient={props.addIngredient} />
            </ul>
        </div>
    );
};

export default AddIngredient;